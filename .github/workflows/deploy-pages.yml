# Statyczny build → GitHub Pages (darmowy hosting pod adresem user.github.io/nazwa-repo/).
# Formularz wymaga osobnego API: ustaw zmienną VITE_CONTACT_API w Settings → Secrets and variables → Actions → Variables.

name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Ustal ścieżkę Vite (base)
        id: base
        shell: bash
        run: |
          NAME="${{ github.event.repository.name }}"
          if [[ "$NAME" == *.github.io ]]; then
            echo "path=/" >> "$GITHUB_OUTPUT"
          else
            echo "path=/$NAME/" >> "$GITHUB_OUTPUT"
          fi

      - name: Zainstaluj i zbuduj
        env:
          VITE_BASE_PATH: ${{ steps.base.outputs.path }}
          VITE_CONTACT_API: ${{ vars.VITE_CONTACT_API }}
        run: npm ci && npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
